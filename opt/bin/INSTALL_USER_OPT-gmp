#!/bin/bash

cd /tmp
wget https://gmplib.org/download/gmp/gmp-6.0.0a.tar.xz
tar xfv `find . -type f -name 'gmp*'`
cd `find . -type d -name 'gmp*'`
./confiture --prefix=/home/`id -un`/opt
make
make install

